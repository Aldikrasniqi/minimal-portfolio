import { PageLoading } from '@/components/ui/Loading'

/**
 * Loading UI component for Next.js app router
 * Displays during page navigation and loading states
 */
export default function Loading() {
	return <PageLoading />
}
